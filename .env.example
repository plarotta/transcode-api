# App
APP_ENV=development
SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
BASE_URL=http://localhost:8000

# Database
DATABASE_URL=sqlite+aiosqlite:///./transcode.db

# Storage (local for MVP — absolute or relative path)
STORAGE_DIR=./storage

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...

# FFmpeg
# FFMPEG_PATH=ffmpeg          # override if ffmpeg is not on PATH
# FFPROBE_PATH=ffprobe        # override if ffprobe is not on PATH
# MAX_CONCURRENT_JOBS=4       # defaults to os.cpu_count() — tune for your VPS
# MAX_VIDEO_DURATION_SECONDS=3600
# FFMPEG_PRESET=ultrafast     # ultrafast|superfast|veryfast|faster|fast

# Credits
# CREDITS_PER_MINUTE=10
# CREDIT_PACK_PRICE_USD=500   # in cents ($5.00)
# CREDIT_PACK_CREDITS=1000
